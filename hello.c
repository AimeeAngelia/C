#include <stdio.h>

int main() {
       printf("Hello, World!\n");         // This is a simple C program that prints "Hello, World!" to the console.
       return 0;                    // The return statement is used to exit the main function and return a value to the operating system. In this case, the value is 0, which indicates that the program executed successfully.
}